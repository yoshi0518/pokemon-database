{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "fmt": {},
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    },
    "prebuild": {},
    "build": {
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "dependsOn": ["^prebuild"]
    },
    "start": {},
    "storybook": {}
  }
}
