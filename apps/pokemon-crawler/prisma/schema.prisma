//
// Autogenerated by `prisma-import`
// Any modifications will be overwritten on subsequent runs.
//

//
// base.prisma
//

// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

//
// pokemon.prisma
//

model Pokemon {
  id                     Int      @id
  name                   String   @unique @db.VarChar(500)
  baseExperience         Int?     @map("base_experience")
  height                 Int?
  isDefault              Boolean? @map("is_default")
  order                  Int?
  weight                 Int?
  locationAreaEncounters String?  @map("location_area_encounters") @db.VarChar(500)
  createdAt              DateTime @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt              DateTime @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")

  @@map("t_pokemon")
}
