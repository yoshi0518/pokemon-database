//
// Autogenerated by `prisma-import`
// Any modifications will be overwritten on subsequent runs.
//

//
// base.prisma
//

// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  url          = env("DATABASE_URL")
  relationMode = "prisma"
}

//
// pokemon.prisma
//

model CrawlerAbility {
  id                Int                        @id
  name              String?                    @db.VarChar(500)
  generation        String?                    @db.VarChar(100)
  is_main_series    Boolean?
  createdAt         DateTime                   @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt         DateTime                   @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  abilityNames      CrawlerAbilityNames[]
  abilityEffect     CrawlerAbilityEffect[]
  abilityFlavorText CrawlerAbilityFlavorText[]

  @@map("t_crawler_ability")
}

model CrawlerAbilityNames {
  id        Int
  language  String         @db.VarChar(100)
  name      String?        @db.VarChar(500)
  createdAt DateTime       @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt DateTime       @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  ability   CrawlerAbility @relation(fields: [id], references: [id])

  @@id([id, language])
  @@map("t_crawler_ability_names")
}

model CrawlerAbilityEffect {
  id          Int
  language    String         @db.VarChar(100)
  effect      String?        @db.Text
  shortEffect String?        @map("short_effect") @db.VarChar(500)
  createdAt   DateTime       @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt   DateTime       @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  ability     CrawlerAbility @relation(fields: [id], references: [id])

  @@id([id, language])
  @@map("t_crawler_ability_effect")
}

model CrawlerAbilityFlavorText {
  id           Int
  language     String         @db.VarChar(100)
  versionGroup String         @map("version_group") @db.VarChar(100)
  flavorText   String?        @map("flavor_text") @db.VarChar(500)
  createdAt    DateTime       @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt    DateTime       @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  ability      CrawlerAbility @relation(fields: [id], references: [id])

  @@id([id, language, versionGroup])
  @@map("t_crawler_ability_flavor_text")
}

model CrawlerCharacteristics {
  id                            Int                                    @id
  highest_stat                  String?                                @db.VarChar(500)
  gene_modulo                   Int?
  createdAt                     DateTime                               @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt                     DateTime                               @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  characteristicsDescriptions   CrawlerCharacteristicsDescriptions[]
  characteristicsPossibleValues CrawlerCharacteristicsPossibleValues[]

  @@map("t_crawler_characteristics")
}

model CrawlerCharacteristicsDescriptions {
  id              Int
  language        String                 @db.VarChar(100)
  description     String?                @db.VarChar(500)
  createdAt       DateTime               @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt       DateTime               @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  characteristics CrawlerCharacteristics @relation(fields: [id], references: [id])

  @@id([id, language])
  @@map("t_crawler_characteristics_descriptions")
}

model CrawlerCharacteristicsPossibleValues {
  id              Int
  row_no          Int
  possible_value  Int?
  createdAt       DateTime               @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt       DateTime               @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  characteristics CrawlerCharacteristics @relation(fields: [id], references: [id])

  @@id([id, row_no])
  @@map("t_crawler_characteristics_possible_values")
}

model CrawlerEggGroup {
  id            Int                    @id
  name          String?                @db.VarChar(500)
  createdAt     DateTime               @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt     DateTime               @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  eggGroupNames CrawlerEggGroupNames[]

  @@map("t_crawler_egg_group")
}

model CrawlerEggGroupNames {
  id        Int
  language  String          @db.VarChar(100)
  name      String?         @db.VarChar(500)
  createdAt DateTime        @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt DateTime        @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  eggGroup  CrawlerEggGroup @relation(fields: [id], references: [id])

  @@id([id, language])
  @@map("t_crawler_egg_group_names")
}

model CrawlerGender {
  id        Int      @id
  name      String?  @db.VarChar(500)
  createdAt DateTime @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt DateTime @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")

  @@map("t_crawler_gender")
}

model CrawlerGrowthRate {
  id               Int                       @id
  name             String?                   @db.VarChar(500)
  createdAt        DateTime                  @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt        DateTime                  @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  growthRateLebels CrawlerGrowthRateLevels[]

  @@map("t_crawler_growth_rate")
}

model CrawlerGrowthRateLevels {
  id         Int
  level      Int               @db.SmallInt
  experience Int?
  createdAt  DateTime          @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt  DateTime          @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  growthRate CrawlerGrowthRate @relation(fields: [id], references: [id])

  @@id([id, level])
  @@map("t_crawler_growth_rate_levels")
}

model CrawlerNature {
  id                     Int                             @id
  name                   String?                         @db.VarChar(500)
  decreased_stat         String?                         @db.VarChar(100)
  increased_stat         String?                         @db.VarChar(100)
  hates_flavor           String?                         @db.VarChar(100)
  likes_flavor           String?                         @db.VarChar(100)
  createdAt              DateTime                        @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt              DateTime                        @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  natureNames            CrawlerNatureNames[]
  natureMoveBattleStyles CrawlerNatureMoveBattleStyles[]

  @@map("t_crawler_nature")
}

model CrawlerNatureNames {
  id        Int
  language  String        @db.VarChar(100)
  name      String?       @db.VarChar(500)
  createdAt DateTime      @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt DateTime      @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  nature    CrawlerNature @relation(fields: [id], references: [id])

  @@id([id, language])
  @@map("t_crawler_nature_names")
}

model CrawlerNatureMoveBattleStyles {
  id                 Int
  name               String        @db.VarChar(500)
  high_hp_preference Int?
  low_hp_preference  Int?
  createdAt          DateTime      @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt          DateTime      @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  nature             CrawlerNature @relation(fields: [id], references: [id])

  @@id([id, name])
  @@map("t_crawler_nature_move_battle_styles")
}

model CrawlerPokeathlonStat {
  id                                     Int                                             @id
  name                                   String?                                         @db.VarChar(500)
  createdAt                              DateTime                                        @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt                              DateTime                                        @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  pokeathlonStatNames                    CrawlerPokeathlonStatNames[]
  pokeathlonStatAffectingNaturesDecrease CrawlerPokeathlonStatAffectingNaturesDecrease[]
  pokeathlonStatAffectingNaturesIncrease CrawlerPokeathlonStatAffectingNaturesIncrease[]

  @@map("t_crawler_pokeathlon_stat")
}

model CrawlerPokeathlonStatNames {
  id             Int
  language       String                @db.VarChar(100)
  name           String?               @db.VarChar(500)
  createdAt      DateTime              @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt      DateTime              @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  pokeathlonStat CrawlerPokeathlonStat @relation(fields: [id], references: [id])

  @@id([id, language])
  @@map("t_crawler_pokeathlon_stat_names")
}

model CrawlerPokeathlonStatAffectingNaturesDecrease {
  id             Int
  name           String                @db.VarChar(500)
  maxChange      Int?                  @map("max_change")
  createdAt      DateTime              @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt      DateTime              @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  pokeathlonStat CrawlerPokeathlonStat @relation(fields: [id], references: [id])

  @@id([id, name])
  @@map("t_crawler_pokeathlon_stat_affecting_natures_decrease")
}

model CrawlerPokeathlonStatAffectingNaturesIncrease {
  id             Int
  name           String                @db.VarChar(500)
  maxChange      Int?                  @map("max_change")
  createdAt      DateTime              @default(dbgenerated("NOW(3)")) @map("created_at")
  updatedAt      DateTime              @default(dbgenerated("NOW(3) ON UPDATE NOW(3)")) @map("updated_at")
  pokeathlonStat CrawlerPokeathlonStat @relation(fields: [id], references: [id])

  @@id([id, name])
  @@map("t_crawler_pokeathlon_stat_affecting_natures_increase")
}
